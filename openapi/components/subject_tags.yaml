title: Tags
type: array
items:
  title: Tag
  required:
    - name
    - count
    - total_count
  type: object
  properties:
    name:
      title: Name
      type: string
    count:
      title: Count
      description: 使用此标签标记本条目的用户数
      type: integer
    total_count:
      title: Total Count
      description: 此标签在所有条目中的使用总次数
      type: integer
